import React from 'react';
import { render } from '@testing-library/react';
import Popup from './Popup';
import mockContent from './Popup.mock';

describe('<Popup />', () => {
  describe('<Popup /> renders correctly', () => {
    test.todo('write tests');
  });
});
